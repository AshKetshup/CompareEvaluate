#ifndef TESTMANAGER_H
#define TESTMANAGER_H

#include <filesystem>
#include <string>
#include "yaml/yaml.h"

using namespace std;

class TestManager {
    public:
        TestManager(string name, filesystem::path dirPath);


    private:


};

#endif // TESTMANAGER_H
